<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_peachtree_export_wizard_form" model="ir.ui.view">
        <field name="name">peachtree.export.wizard.form</field>
        <field name="model">peachtree.export.wizard</field>
        <field name="arch" type="xml">
            <form string="Export Sales to Peachtree">
                <group invisible="state != 'draft'">
                    <group string="Date Range">
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                    <group string="Options">
                        <field name="only_unexported"/>
                        <field name="include_credit_notes"/>
                        <field name="mark_as_exported"/>
                    </group>
                </group>
                <group invisible="state != 'done'">
                    <group>
                        <field name="export_count"/>
                        <field name="csv_file" filename="csv_filename"/>
                        <field name="csv_filename" invisible="1"/>
                    </group>
                </group>
                <field name="state" invisible="1"/>
                <footer>
                    <button name="action_export"
                            string="Export to CSV"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_reset"
                            string="Export Again"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'done'"/>
                    <button name="action_reset_exported_flag"
                            string="Reset Exported Flag"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'draft'"
                            confirm="This will mark all exported invoices in the selected date range as unexported. Continue?"/>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_peachtree_export_wizard" model="ir.actions.act_window">
        <field name="name">Export to Peachtree</field>
        <field name="res_model">peachtree.export.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
